// criando e especificando o tipos dos valores dentro do array no typescript.

let arr: string[] = ["Diogo", "Couto", "Santos",]; // toda vez que eu criar um array de string utilizo : string[]

let num: number[] = [1, 2, 3, 4, 5];  // toda vez que eu criar um array de numeors utilizo : number[]
let numeros: Array<number> =[1, 2, 3, 4]; // outra forma de declarar array no Tsc ... serve para todos os tipos de dados.













/* Para converter o código  - terminal , digite tsc nome do arquivo "script.ts" */
// toda vez que for testar o código , devemos converter ele .